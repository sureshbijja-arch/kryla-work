---
idea: Members can custom design each of the 6 page sections (Header, Offerings, Highlights, MemberTalk, FAQ, GetInTouch) — changing background, adding photos/video/animations/moving frames, editing text, colors, font
goal: harden
status: complete
updated: 2026-06-30T12:33
---

- (note) User motivation: members share their Kryla page as their business face; want to own it, decorate it, feel pride in it. Goal is to encourage interactivity and make it their operating homepage.
- (lock) LOCK: Design freedom must be constrained so bad output is structurally impossible. Member pride/ownership AND business quality are both required outcomes. Unlimited is not the path.
- (kill) KILL: Background video - perf budget, CDN cost, bad-output risk too high for v1.
- (kill) KILL: Per-section text editing - already covered by AI chat editor, not an additional surface.
- (lock) LOCK: Sections confirmed as hero/services/highlights/bio/faq/contact (matches codebase). Background photo allowed on hero, services, highlights, bio only. FAQ and contact get no background photo (text readability, form usability).
- (lock) LOCK: Section styling lives in the existing Page Layout tab (SectionsTab), not Services tab, not a new tab. Style controls expand inline on each section card alongside the existing variant picker.
- (crack) CRACK: 4 palette-relative presets may feel too limited to give members genuine ownership feeling. Need more freedom while still guaranteeing quality output.
- (lock) LOCK: Background freedom = 16 curated colour options (auto-contrast text) + any photo upload with auto-scrim on hero/services/highlights/bio. Moving frames on hero: toggle on/off + count (1/2/3). No hex picker, no gradient builder, no per-section font or accent override.
- (lock) LOCK: Plan gating = Sprout and above. Seed members see controls greyed out with upgrade prompt (drives awareness and upgrades).
- (lock) LOCK: Moving frames = Grow+ only (gallery dependency). Feature ladder: Seed = greyed controls + upgrade prompt. Sprout+ = 16 bg colour presets + photo upload (hero/services/highlights/bio). Grow+ = adds moving frames on hero (1-3 frames from gallery).
