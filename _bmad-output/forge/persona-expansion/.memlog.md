---
idea: Add more personas (advocate, retailer, etc.), allow custom persona entry in onboarding, and immediately generate a template for new personas
goal: harden
updated: 2026-06-29T02:03
---

- (assumption) Immediately = Claude auto-generates template on-the-fly for custom persona, not manual Kryla team curation
- (crack) Unclear whether custom persona template is global (all members) or per-member (generated fresh each time)
- (lock) Custom persona templates are global — once generated, available to all future members who pick that persona
- (lock) Custom persona fallback = default persona template. Member gets a working page immediately; AI-generated custom template replaces it later (pending review or generation).
- (lock) Custom template generation is IMMEDIATE during onboarding — not async, not gated. Member's first page already uses the custom-generated template, not the default.
- (lock) Architecture: persona_templates table in Supabase stores generated templates. Dedup: if persona is already generating (status=generating), second member waits/polls until status=ready before their build-page fires.
- (lock) Generation UX: Option Y. Onboarding completes instantly with default template. Inngest generates custom template async in background. revalidatePath swaps it in when ready. No wait on submit.
- (lock) Member notification: Option 3 — in-app banner in My Space panel shows 'Your page is being personalized...' until Inngest job completes and revalidatePath fires.
